/**
 * Future: decode raster / ImageData into terrain[y][x] placeholder tokens ("plains", "water", …).
 * @param {unknown} imageData
 * @returns {string[][]}
 */
export function imageToTerrain(imageData) {
  void imageData;
  throw new Error("imageToTerrain not implemented");
}
