CTU asset pipeline � consolidated report (generated by implementation + `asset-pipeline:analyze`)

STEP 0 � Current state (see also tools/asset_pipeline_analysis.json)
- assets/: guns, buildings, tiles, obstacles, units, vfx, ui, New_Arrivals (large review queue), effects (new), maps, props, archive.
- attached_assets/: CraftPix packs, light_tank sequences (source copies retained after VFX promotion).
- Root New_Arrivals/: not used; ingest uses assets/New_Arrivals/.

STEP 1 � Target layout (enforced by suggestedDestRelFromMetadata + librarian)
- assets/units/vehicles, assets/units/infantry
- assets/tiles/terrain/{urban,desert,grass}, legacy assets/tiles/{theme}
- assets/tiles/structures/{footprint}, legacy assets/buildings/*
- assets/effects/{explosions,muzzle,smoke}, legacy assets/vfx
- assets/ui/*

STEP 2 � CLIP
- tools/smart_catalog.mjs � @xenova/transformers zero-shot; CACHE_VERSION bumped to 4 (new dest paths invalidate cache).

STEP 3 � Metadata
- Per-raster .ctu.asset.json (merged into assetManifest as asset.ctu). Rules + CLIP suggestions via tools/asset_metadata.mjs.

STEP 4 � Auto-organize
- npm run catalog-assets � New_Arrivals ? destinations from CTU + suggestedDestRelFromMetadata.
- node tools/asset_pipeline_ingest.mjs organize [--dry-run] � optional sweep of existing sidecars vs suggested paths.

STEP 5 � Special cases
- tools/sprite_sheet_slice.mjs � sprite sheets (unchanged).
- Functional brain � misfiled VFX re-homed under assets/effects/<subfolder> (not only assets/vfx).

STEP 6 � Legacy paths
- Light tank VFX copied to assets/effects/...; js/config/spriteAnimations.json points at structured paths (attached_assets copies kept for packs).
- Full removal of attached_assets/ references is not done (spriteAnimations, fxLayer, scenarios, css still reference packs).

STEP 7 � Latest tool outputs
- tools/asset_pipeline_analysis.json � counts, duplicate basename sample, CLIP coarse-family map.
- tools/asset_pipeline_duplicates.json � identical file content (sha256) groups under assets/.
- promote-light-tank-vfx: 12 PNGs ? assets/effects/muzzle/light_tank/* and assets/effects/explosions/light_tank/dead/
