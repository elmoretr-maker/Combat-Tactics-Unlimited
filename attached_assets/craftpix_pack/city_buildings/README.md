# City buildings

## Expected contents

- Building facades, rooftops, destructible structures, and props that read as **structures** rather than repeating ground tiles.
- Often sourced from the **PNG City** / **PNG City 2** packs — move or duplicate here only if you want a clear split from `city/` tile art.

Optional layout: `residential/`, `industrial/`, `props/` once you know the pack filenames.
