# Effects (CraftPix bundle)

## Expected contents

- **`Effects`** folder from the mega-set — muzzle flashes, impacts, generic VFX sheets not already sorted into `smoke/` or `fx/`.

If something is clearly **explosion-only**, you may mirror copies under `fx/` later for the game loader; this folder keeps a 1:1 home for the pack’s `Effects` directory.
