# Vehicles (ground)

## Expected contents

- **`ACS Tank with Damage`** — place under `acs_tank_damage/` (created below) or flatten into `vehicles/` if you prefer.

Other ground vehicles from the mega-set can get sibling subfolders (e.g. `apc/`) as you import them.
